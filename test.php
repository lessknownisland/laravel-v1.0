<?php
// 测试三元运算符
// $username = $name ?: "nobody"; 如果 $name 为空则返回 nobody，如果 $name 不存在，也会报错；
// $username = $name ?? "nobody"; 如果 $name 为空则返回 nobody，如果 $name 不存在，也会返回 nobody；

// $name = "Arno De";
// // $username = $name ?? "nobody";
// // $username = isset($name1) ? $name1 : "nobody";
// // $username = $name1 ?: "nobody";
// $username = $name1 ?? "nobody";

// echo "Hello $username";
// echo "</br>";
// var_dump($username);
// echo "</br>";
phpinfo();


?>